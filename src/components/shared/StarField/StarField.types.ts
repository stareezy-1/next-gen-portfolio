/**
 * Public props for the {@link StarField} hero background.
 */
export interface StarFieldProps {
  /**
   * Optional class applied to the absolutely-positioned container. Use it to
   * layer, mask, or fade the canvas from the consuming surface's stylesheet
   * (e.g. the hero's `hero-starfield` class) without touching the component.
   */
  className?: string;
}
