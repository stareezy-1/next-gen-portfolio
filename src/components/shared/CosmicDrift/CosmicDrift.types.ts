/**
 * Public props for the {@link CosmicDrift} 404 vignette.
 */
export interface CosmicDriftProps {
  /**
   * Optional class applied to the absolutely-positioned, click-through
   * container so the consuming surface can size/mask the canvas.
   */
  className?: string;
}
