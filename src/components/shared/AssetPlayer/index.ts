/**
 * AssetPlayer barrel.
 *
 * Re-exports the {@link AssetPlayer} component and its {@link AssetPlayerProps}
 * type so consumers can import from `@/components/shared/AssetPlayer`.
 *
 * @see Requirements 25.1–25.7, 25.9, 25.10
 */

export { AssetPlayer } from "./AssetPlayer";
export type { AssetPlayerProps } from "./AssetPlayer.types";
